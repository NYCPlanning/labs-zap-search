{{yield (hash
  filterIsActive=this.filterIsActive
  delegate-mutation=this.delegateMutation
  mutateWithAction=this.mutateWithAction
  filter-wrapper=(component 'filter-wrapper'
    filterNames=this.filterNames
    appliedFilters=this.appliedFilters
    filterIsActive=this.filterIsActive
    mutateWithAction=this.mutateWithAction
  )
)}}
