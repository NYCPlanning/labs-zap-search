<FaIcon
  @icon={{(if this.mainProperty 'check-square' 'square')}}
  @prefix={{(unless this.mainProperty 'far')}}
  @class={{(if this.mainProperty 'dark-gray' 'light-gray')}} />
  
  {{this.label}}

{{yield}}
