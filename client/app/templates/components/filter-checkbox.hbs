<FaIcon
  @icon={{if (includes value currentValues) 'check-square' 'square'}}
  @prefix={{unless (includes value currentValues) 'far'}}
  class={{if (includes value currentValues) 'dark-gray' 'light-gray'}}
/>

{{value}}

{{yield}}
