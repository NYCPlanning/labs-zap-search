<RangeSlider
  @start={{this.start}}
  @min={{this.min}}
  @max={{this.max}}
  @connect={{true}}
  @tooltips={{array this.format this.format}}
  step={{1}}
  behaviour={{"drag"}}
  @on-change={{this.sliderChanged}}
  />
