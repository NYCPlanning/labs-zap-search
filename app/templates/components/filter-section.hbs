{{yield (hash
  delegate-mutation=(action 'delegateMutation')
  filter-wrapper=(component 'filter-wrapper'
    filterNames=filterNames
    appliedFilters=appliedFilters
    filterTitle=filterTitle
    tooltip=tooltip
    mutateWithAction=(action 'mutateWithAction')
  )
)}}
