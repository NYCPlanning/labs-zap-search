{{fa-icon
  (if (contains value currentValues) 'check-square' 'square')
  prefix=(unless (contains value currentValues) 'far')
  class=(if (contains value currentValues) 'dark-gray' 'light-gray')
}}

{{value}}

{{yield}}
