{{fa-icon
  (if (contains this.value this.currentValues) 'check-square' 'square')
  prefix=(unless (contains this.value this.currentValues) 'far')
  class=(if (contains this.value this.currentValues) 'dark-gray' 'light-gray')
}}

{{this.value}}

{{yield}}
