<fieldset class="medium-margin-bottom vote-fieldset">
  <legend><strong>Recommendation</strong></legend>
  {{#power-select
    searchEnabled=false
    options=recOptions
    placeholder="Select a time"
    onchange=(action (mut this.recommendation))
    as |option|
  }}
    {{option.label}}
  {{/power-select}}
</fieldset>

<fieldset class="medium-margin-bottom vote-fieldset">
  <legend></legend>
  <textarea></textarea>
</fieldset>

{{#if (not-eq participantType 'BP')}}
  {{!-- vote counts --}}
  <input type="number" />
  <input type="number" />
  <input type="number" />
  <input type="number" />
{{/if}}

{{yield}}
